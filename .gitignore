# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Entornos / secretos
.env
.env.*
!.env.example
!.env.prod.example

# Llaves SSH de despliegue (privadas — nunca versionar)
.ssh/

# Runtime de los scripts de administracion (scripts/manage.ps1)
.logs/

# Flutter / Dart (Incremento 3)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/build/
*.iml

# Node / web-admin (Incremento 4)
node_modules/
.next/
.vite/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Worktrees de agentes + overrides/insumos locales (no versionar)
.claude/
# Insumos/back-ups locales de branding (zips de logos entregados + respaldos)
/_backup-logos-viejos-*
/nuevos logos.zip
infra/compose/docker-compose.lan.yml
resultados_paginas/

# Trabajo humano-Claude / prompts: SOLO local, fuera del repo (y del historial).
# Información de colaboración, no del producto.
/CLAUDE.md
/prompt.txt
/docs/change-requests/PLAN-EJECUCION-AGENTES.md

# Red de seguridad. El material operativo con valores reales del servidor (IP, rutas,
# panel del registrador, DNS) NO vive aquí: está FUERA del árbol del repositorio.
# Esta regla solo existe por si alguien lo vuelve a copiar dentro por error.
# La versión reproducible y sin datos del host vive en docs/despliegue/.
/.local-docs/
