import 'package:flutter/widgets.dart';

/// Stub no-web: el botón GIS solo existe en navegador. Nunca se muestra fuera de web (el login
/// nativo usa `GoogleAuthService.signIn()`).
Widget googleSignInButton() => const SizedBox.shrink();
